----------------
Movies Catalogue
----------------
Django application, with following REST APIs:
GET /movies/toprated
GET /movies/search
POST /movie
POST /actor
POST /director
POST /genre

Steps to run:
> Setup the virtualenv and install the dependencies.
    $ pip install -r requirements.txt
> Apply migrations
    $ python manage.py migrate
    $ python manage.py makemigrations movieapp
> Run the server
    $ python manage.py runserver


